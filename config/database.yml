# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: scm_analysis_development
  username: root
  socket: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: poker_analysis_test
  username: root
  password: applegoat543
  socket: /tmp/mysql.sock
  
  
# WARNING: I think the socket for production must be different than tmp/mysql.sock
production:
  adapter: mysql
  database: scm_analysis_production
  username: augie
  password: augie@03172010
  socket: /var/run/mysqld/mysqld.sock
  